<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:background="@drawable/list_row_selector" >

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="64dp"
        android:src="@drawable/shape_rec_list_row" >
    </ImageView>

    <ImageView
        android:id="@id/drag"
        android:layout_width="24dp"
        android:layout_height="64dp"
        android:scaleType="fitCenter"
        android:src="@drawable/for_drag" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginBottom="6dp"
        android:layout_marginTop="6dp"
        android:layout_toLeftOf="@id/playing_indicator"
        android:layout_toRightOf="@id/drag"
        android:gravity="center_vertical"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/Title"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="6dp"
            android:layout_marginRight="6dp"
            android:maxLines="1"
            android:text="TextView"
            android:textColor="@color/song_title_color"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/Artist"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="6dp"
            android:layout_marginRight="6dp"
            android:maxLines="1"
            android:text="TextView"
            android:textColor="@color/artist_color" 
            android:textSize="12sp"/>
    </LinearLayout>

    <ImageView
        android:id="@id/playing_indicator"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:scaleType="fitCenter"
        android:src="@drawable/av_play" />

</RelativeLayout>